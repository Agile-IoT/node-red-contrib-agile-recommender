{
  "name": "node-red-contrib-agile-recommender",
  "version": "1.0.0",
  "description": "This is a package for Node Red nodes meant for the AGILE recommender and the configurator.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "node-red"     : {
       "nodes": {
           "recommender": "./recommender.js"
       }
   }
}
